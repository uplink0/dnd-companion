(() => {
  const CAMPAIGN_ID='10000000-0000-4000-8000-000000000001';
  const ACTIVE_KEY='dnd.activeCharacterId';
  const app=document.querySelector('#app');
  const crumb=document.querySelector('#crumb');
  const esc=(value)=>String(value??'').replace(/[&<>\"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const fmt=(value)=>new Intl.NumberFormat('ru-RU').format(Number(value||0));
  const activeId=()=>localStorage.getItem(ACTIVE_KEY)||null;
  const setActive=(id)=>{localStorage.setItem(ACTIVE_KEY,id);window.dispatchEvent(new CustomEvent('dnd:active-character-changed',{detail:{id}}));};
  const colors={'Элдрик':['#345c72','#0c2937','✦'],'Лира':['#6d4842','#251c22','☾'],'Бром':['#6b533b','#272119','⚒'],'Селена':['#56506e','#211e30','☼']};
  const RACES=['Человек','Высший эльф','Дварф','Дроу','Полуорк','Полурослик'];
  const CLASSES=['Воин','Следопыт','Вор','Чародей','Волшебник','Жрец','Паладин','Варвар'];
  const STAT_KEYS=[['str','СИЛ'],['dex','ЛОВ'],['con','ТЕЛ'],['int','ИНТ'],['wis','МДР'],['cha','ХАР']];

  async function api(path,options={}){const response=await fetch(path.startsWith('/api/')?path:`/api${path}`,{headers:{'Content-Type':'application/json'},...options});const data=await response.json();if(!response.ok)throw new Error(data.error||'Ошибка сервера');return data}
  function mods(abilities){return STAT_KEYS.map(([key,label])=>{const score=Number(abilities?.[key]??10);const mod=Math.floor((score-10)/2);return{label,score,mod:mod>=0?`+${mod}`:String(mod)}})}
  function visual(c,big=false){const [a,b,rune]=colors[c.name]||['#3b5360','#0d202c','✦'];return `<div class="${big?'character-big-visual':'hero-visual'}" style="--hero-a:${a};--hero-b:${b};--a:${a};--b:${b}" data-rune="${rune}">${big?`<span class="character-big-rune">${rune}</span>`:''}</div>`}

  function characterCard(c){const selected=c.id===activeId();const pct=c.hp_max?Math.round(c.hp/c.hp_max*100):0;return `<article class="card character-card ${selected?'selected':''}" data-character-id="${esc(c.id)}" tabindex="0" role="button" aria-label="Открыть персонажа ${esc(c.name)}">${selected?'<span class="character-badge">Текущий герой</span>':''}${visual(c)}<div class="card-body"><h3>${esc(c.name)}</h3><div class="meta">${esc(c.class_name)} · ${c.level} уровень</div><div class="stat-row"><span>Здоровье</span><b>${c.hp} / ${c.hp_max}</b></div><div class="bar"><span style="width:${pct}%"></span></div><div class="tags"><span class="tag">${esc(c.race)}</span><span class="tag">Ур. ${c.level}</span><span class="tag">Игрок</span></div><div class="character-open-hint"><span>Открыть лист персонажа</span><strong>→</strong></div></div></article>`}

  function addHeroCard(){return `<article class="card add-character-card" data-add-character tabindex="0" role="button" aria-label="Добавить героя"><div class="add-character-plus">+</div><div class="add-character-label">добавить героя</div></article>`}

  async function listCharacters(){
    try{
      const chars=await api(`/api/characters?campaignId=${CAMPAIGN_ID}`);
      app.innerHTML=`<header class="page-head"><div><div class="eyebrow">Текущая партия</div><h1>Персонажи</h1><p>Выбери героя, чтобы открыть его личный лист и продолжить игру с его историей.</p></div></header><section class="grid characters">${chars.map(characterCard).join('')}${addHeroCard()}</section>`;
      crumb.textContent='Персонажи';wireCharacterCards();document.querySelector('[data-add-character]')?.addEventListener('click',renderCreator);
    }catch(error){app.innerHTML=`<div class="panel" style="padding:24px">Не удалось загрузить персонажей: ${esc(error.message)}</div>`}
  }

  function creatorMarkup(draft=null){
    const abilities=draft?.abilities||{str:12,dex:12,con:12,int:12,wis:12,cha:12};
    const sum=STAT_KEYS.reduce((s,[key])=>s+Number(abilities[key]||0),0);
    return `<div class="character-creator"><div class="character-detail-top"><button class="back-button" data-back>← Назад к персонажам</button><div class="creator-total"><b id="statTotal">${sum}</b> / 72 очка</div></div><section class="panel creator-panel"><div class="eyebrow">Новый герой</div><h1>Создание персонажа</h1><p class="creator-help">Распредели ровно 72 базовых очка. Базовое значение каждой характеристики — от 1 до 18. Бонусы расы и класса добавляются сверху и не входят в лимит 18.</p><div class="creator-grid"><label>Имя<input id="heroName" maxlength="80" value="${esc(draft?.name||'')}" placeholder="Имя персонажа"></label><label>Раса<select id="heroRace">${RACES.map(x=>`<option ${draft?.race===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Класс<select id="heroClass">${CLASSES.map(x=>`<option ${draft?.className===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Предыстория<input id="heroBackground" maxlength="120" value="${esc(draft?.background||'')}" placeholder="Например: Искатель древностей"></label></div><div class="stat-editor"><div class="panel-title">Характеристики <span>максимум базы: 18</span></div><div class="creator-stats">${STAT_KEYS.map(([key,label])=>`<label><span>${label}</span><input class="stat-input" data-stat="${key}" type="number" min="1" max="18" value="${Number(abilities[key]||1)}"><small id="mod-${key}"></small></label>`).join('')}</div></div><div class="creator-actions"><button class="btn" data-generate>✦ Сгенерировать персонажа полностью</button><button class="btn gold" data-create>Создать героя</button></div><div id="creatorError" class="creator-error" hidden></div><div class="creator-preview" id="creatorPreview"></div></section></div>`}

  function updateCreatorPreview(){const name=document.querySelector('#heroName')?.value.trim()||'Новый герой';const race=document.querySelector('#heroRace')?.value||'';const className=document.querySelector('#heroClass')?.value||'';const abilities=Object.fromEntries(document.querySelectorAll('.stat-input').map(i=>[i.dataset.stat,Number(i.value)]));const total=STAT_KEYS.reduce((s,[k])=>s+Number(abilities[k]||0),0);const preview=document.querySelector('#creatorPreview');if(preview)preview.innerHTML=`<strong>${esc(name)}</strong><span>${esc(race)} · ${esc(className)}</span><span>Базовые очки: ${total}/72</span>`;STAT_KEYS.forEach(([key])=>{const el=document.querySelector(`#mod-${key}`);if(!el)return;const m=Math.floor((Number(abilities[key])-10)/2);el.textContent=`мод. ${m>=0?`+${m}`:m}`});const totalEl=document.querySelector('#statTotal');if(totalEl){totalEl.textContent=total;totalEl.classList.toggle('invalid',total!==72)}}

  async function renderCreator(draft=null){app.innerHTML=creatorMarkup(draft);crumb.textContent='Создание героя';document.querySelector('[data-back]')?.addEventListener('click',()=>location.hash='characters');document.querySelectorAll('.stat-input').forEach(i=>i.addEventListener('input',updateCreatorPreview));document.querySelector('[data-generate]')?.addEventListener('click',async()=>{try{const generated=await api('/api/characters/generate',{method:'POST',body:'{}'});renderCreator(generated)}catch(error){show(error.message)}});document.querySelector('[data-create]')?.addEventListener('click',createCharacter);updateCreatorPreview()}

  async function createCharacter(){const errorBox=document.querySelector('#creatorError');try{const abilities=Object.fromEntries(document.querySelectorAll('.stat-input').map(i=>[i.dataset.stat,Number(i.value)]));const total=STAT_KEYS.reduce((s,[k])=>s+Number(abilities[k]||0),0);if(total!==72)throw new Error(`Нужно ровно 72 очка. Сейчас: ${total}`);for(const [key] of STAT_KEYS){if(abilities[key]<1||abilities[key]>18)throw new Error(`База ${key.toUpperCase()} должна быть от 1 до 18`) }const created=await api('/api/characters',{method:'POST',body:JSON.stringify({campaignId:CAMPAIGN_ID,name:document.querySelector('#heroName').value.trim(),race:document.querySelector('#heroRace').value,className:document.querySelector('#heroClass').value,background:document.querySelector('#heroBackground').value.trim(),abilities})});setActive(created.id);location.hash=`character/${encodeURIComponent(created.id)}`;}catch(error){errorBox.hidden=false;errorBox.textContent=error.message}}

  async function renderDetail(id){try{const [character,events,messages,journal]=await Promise.all([api(`/api/characters/${encodeURIComponent(id)}/summary`),api(`/api/player/events?campaignId=${CAMPAIGN_ID}&characterId=${encodeURIComponent(id)}`),api(`/api/player/messages?campaignId=${CAMPAIGN_ID}&characterId=${encodeURIComponent(id)}`),api(`/api/player/journal?campaignId=${CAMPAIGN_ID}&characterId=${encodeURIComponent(id)}`)]);const abilities=mods(character.abilities);const active=id===activeId();const inventory=character.inventory||[];const effects=character.effects||[];const recentEvents=(events||[]).slice(-8).reverse();const recentMessages=(messages||[]).slice(-6).reverse();app.innerHTML=`<div class="character-detail"><div class="character-detail-top"><button class="back-button" data-back>← Назад к персонажам</button><button class="start-game" data-start>${active?'Продолжить игру':'Начать игру'}</button></div><section class="character-detail-hero"><div class="panel">${visual(character,true)}</div><div class="panel character-identity-panel"><div class="eyebrow">${active?'Активный герой':'Лист персонажа'}</div><h1>${esc(character.name)}</h1><div class="subtitle">${esc(character.class_name)} · ${esc(character.race)} · ${character.level} уровень</div><p class="bio">${esc(character.biography||'История героя ещё не записана.')}</p><div class="character-tags"><span class="tag">Игрок</span><span class="tag">Ранг: ${esc(character.rank||'—')}</span><span class="tag">XP ${fmt(character.xp)} / ${fmt(character.xp_next)}</span></div><div class="character-stat-grid"><div class="character-stat hp"><span>Здоровье</span><strong>${character.hp} / ${character.hp_max}</strong></div><div class="character-stat"><span>КД</span><strong>${character.armor_class}</strong></div><div class="character-stat"><span>Скорость</span><strong>${character.speed} фт.</strong></div><div class="character-stat"><span>Инициатива</span><strong>${character.initiative>=0?`+${character.initiative}`:character.initiative}</strong></div></div></div></section><section class="detail-grid"><article class="panel"><div class="panel-title">Характеристики <span>БМ +${character.proficiency_bonus}</span></div><div class="ability-grid">${abilities.map(a=>`<div class="ability-cell"><span class="ability-name">${a.label}</span><strong class="ability-score">${a.score}</strong><span class="ability-mod">${a.mod}</span></div>`).join('')}</div><div class="resource-grid"><div class="resource"><small>Пассивная внимательность</small><strong>${character.passivePerception}</strong></div><div class="resource"><small>КС заклинаний</small><strong>${character.spell_save_dc??'—'}</strong></div><div class="resource"><small>Атака заклинанием</small><strong>${character.spell_attack_bonus!=null?(character.spell_attack_bonus>=0?`+${character.spell_attack_bonus}`:character.spell_attack_bonus):'—'}</strong></div><div class="resource"><small>Вдохновение</small><strong>${character.inspiration?'Да':'Нет'}</strong></div></div></article><article class="panel"><div class="panel-title">Ресурсы и состояние</div><div class="resource-grid"><div class="resource"><small>Временные HP</small><strong>${character.temp_hp}</strong></div><div class="resource"><small>Кости хитов</small><strong>${esc(character.hit_dice)}</strong></div><div class="resource"><small>Состояния</small><strong class="${effects.length?'danger-state':'success-state'}">${effects.length?esc(effects.map(e=>e.name).join(', ')):'В норме'}</strong></div><div class="resource"><small>Золото</small><strong>${fmt(character.currency?.gp||0)} gp</strong></div></div></article><article class="panel"><div class="panel-title">Инвентарь <span>${inventory.length}</span></div><div class="inventory-list">${inventory.length?inventory.map(i=>`<div class="inventory-row"><div class="inventory-icon">◈</div><div class="inventory-main"><strong>${esc(i.name)} ×${i.quantity}</strong><small>${esc(i.rarity)} · ${esc(i.item_type)}</small></div></div>`).join(''):'<div class="state-note">Инвентарь пуст.</div>'}</div></article><article class="panel"><div class="panel-title">Последние события героя <span>${events.length}</span></div><div class="event-list">${recentEvents.length?recentEvents.map(e=>`<div class="event-row"><i class="event-dot"></i><div><strong>${esc(e.event_type)}</strong><p>${esc(JSON.stringify(e.payload||{}))}</p><small>${new Date(e.created_at).toLocaleString('ru-RU')}</small></div></div>`).join(''):'<div class="state-note">У героя пока нет записанных событий.</div>'}</div></article><article class="panel"><div class="panel-title">История чата <span>${messages.length}</span></div><div class="event-list">${recentMessages.length?recentMessages.map(m=>`<div class="event-row"><i class="event-dot"></i><div><strong>${m.role==='PLAYER'?'Игрок':'Мастер'}</strong><p>${esc(m.body)}</p><small>${new Date(m.created_at).toLocaleString('ru-RU')}</small></div></div>`).join(''):'<div class="state-note">История этого героя ещё пуста.</div>'}</div></article><article class="panel"><div class="panel-title">Личная летопись <span>${journal.length}</span></div><div class="event-list">${journal.length?journal.slice(0,6).map(j=>`<div class="event-row"><i class="event-dot"></i><div><strong>${esc(j.title)}</strong><p>${esc(j.body)}</p><small>${new Date(j.created_at).toLocaleString('ru-RU')}</small></div></div>`).join(''):'<div class="state-note">Записей героя пока нет.</div>'}</div></article></section></div>`;crumb.textContent=character.name;document.querySelector('[data-back]')?.addEventListener('click',()=>location.hash='characters');document.querySelector('[data-start]')?.addEventListener('click',()=>startGame(id));}catch(error){app.innerHTML=`<div class="panel" style="padding:24px">Не удалось открыть героя: ${esc(error.message)}</div>`}}

  function wireCharacterCards(){document.querySelectorAll('.character-card').forEach(card=>{const open=()=>location.hash=`character/${encodeURIComponent(card.dataset.characterId)}`;card.addEventListener('click',open);card.addEventListener('keydown',(event)=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();open()}})})}
  function startGame(id){setActive(id);location.hash='chat'}
  function show(message){const t=document.querySelector('#toast');if(!t)return;t.textContent=message;t.classList.add('show');clearTimeout(window.__characterToast);window.__characterToast=setTimeout(()=>t.classList.remove('show'),2600)}
  async function route(){const hash=location.hash||'#chat';if(hash==='#characters'){await listCharacters();return true}const match=hash.match(/^#character\/([^/]+)$/);if(match){await renderDetail(decodeURIComponent(match[1]));return true}return false}
  window.dndCharacterFlowRoute=route;window.addEventListener('hashchange',()=>setTimeout(route,0));if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(route,0));else setTimeout(route,0);
})();
